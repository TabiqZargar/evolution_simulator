"""Pygame visualization package (imported lazily by the CLI).

Submodules are imported by ``evolution_sim.visualization.app`` when the GUI
mode is requested; the headless CLI never touches this package.
"""
